// The product's name is undecided. This is the ONE place it is spelled out —
// every screen, page title and lockup reads from here rather than a literal
// string, so renaming the product later is a one-line change.
export const APP_NAME = "Hx"
